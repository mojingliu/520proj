#!/bin/sh
#
# jasmin - runs the Jasmin assembler assuming CLASSPATH already set 
# 
# Usage:
#     jasmin [-d <directory>] <filename> [<filename> ...]
#

$JAVADIR/bin/java -classpath $CLASSPATH jasmin.Main $*
